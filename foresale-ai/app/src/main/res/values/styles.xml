<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/design_snackbar_background_color</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/design_snackbar_background_color</item>
        <item name="android:colorForeground">@color/foreground_material_dark</item>
        <item name="android:textColorPrimary">@color/accent_material_light</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ForeSale">

    </style>

    <style name="ForeSale.Input.Text" parent="ForeSale">
        <item name="android:textSize">22sp</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="ForeSale.Button.Text" parent="@android:style/TextAppearance.Material.Widget.Button">
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="ForeSale.LargeText" parent="@android:style/TextAppearance.Material.Large">
        <item name="android:textAllCaps">true</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">44sp</item>
    </style>

</resources>
